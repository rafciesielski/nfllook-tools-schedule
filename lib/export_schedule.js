var MongoClient = require('mongodb').MongoClient;

var exportSchedule = function(schedule) {
    return new Promise((resolve, reject) => {
        //..
        return resolve("OK");
    })
}
module.exports = exportSchedule;
